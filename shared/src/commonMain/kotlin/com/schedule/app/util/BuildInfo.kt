package com.schedule.app.util

// ─── BuildInfo ──────────────────────────────────────────────────────────────
// IsDebugBuild — единственная точка правды "это debug-сборка или нет".
// Используется чтобы прятать экспериментальные штуки (например, тонкую
// настройку анимаций в SettingsScreen) от обычных пользователей релизной
// сборки, оставляя их доступными тебе для тестов.
expect val IsDebugBuild: Boolean
